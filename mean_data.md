# 毕设实验：RAG 效果验证（方案、数据与一次运行结果）

本文档对应仓库内 **`eval/run_ablation.py`**、**`eval/gold_set.json`** 与 **`eval/results/latest.json`**，可直接摘入论文「实验设计」「数据集」「评价指标」「实验结果」等小节；重新跑评估后请同步更新 **第四节表格中的数字**（或改为「见 `eval/results/xxx.json`」引用）。

---

## 一、实验目的

在**不声称临床诊断准确率**的前提下，验证「本地语料检索增强（RAG）」是否在本课题设定下：

1. 使检索返回的片段与**预先标注的期望概念词**更一致（检索侧）；  
2. 使大模型回答与上述期望词所代表的**教材式表述**更一致（生成侧，相对**无 RAG 基线**）。

---

## 二、测试方案（可复现）

### 2.1 对照设置（消融）

对**同一批**合成主诉，在**相同大模型与 API 配置**下各执行一次：

| 条件 | 说明 |
|------|------|
| **有 RAG** | 走完整流水线：分诊 → 关键词 → 向量检索 Top-K → 将摘录注入提示词 → 生成回答。 |
| **无 RAG（基线）** | 仍执行分诊与关键词，但**不向提示词注入**检索摘录（代码参数 `skip_rag=True`）。 |

实现脚本：`eval/run_ablation.py`。在项目根目录执行：

```bash
.venv/bin/python eval/run_ablation.py
```

可选：指定输出路径、请求间隔（减轻限流），例如：

```bash
.venv/bin/python eval/run_ablation.py --out eval/results/论文用_某日期.json --sleep 1.5
```

整站关闭检索（非本消融脚本）：`.env` 中 `RAG_ENABLED=0`（见 `config.py`）。

### 2.2 控制变量（论文中建议写明）

- 模型与端点：与生产环境一致的 `AI_MODEL`、`AI_API_URL`、`AI_API_KEY`（来自 `.env`）。  
- 检索：`RAG_TOP_K`、切块参数、`rag_data` 索引版本与 **`build_rag_index.py`** 所用 embedding 模型一致。  
- 随机性：若生成接口 `temperature > 0`，可注明「存在随机性；本文结果为单次运行」；若需更严，可固定温度后重复 3 次取平均（作展望）。

---

## 三、数据说明

### 3.1 知识库语料（RAG 索引来源）

- **位置**：`corpus/*.txt`（如心血管、呼吸、消化等主题文件）。  
- **格式**：文件头含 `#SOURCE_TITLE`、`#SOURCE_URL_OR_REF`、`#DOMAIN_HINT`、`#LANG` 等；正文为教材式摘录。  
- **索引**：由 `build_rag_index.py` 切块、向量化后写入 `rag_data/index.faiss` 与 `rag_data/meta.json`（具体切块条数以构建日志为准，例如 308 条向量）。  
- **用途**：仅作检索与生成时的参考，**不替代**线下诊疗。

### 3.2 评估金标准题集（测试输入与标注）

- **位置**：`eval/gold_set.json`。  
- **内容**：每条含 `id`、`description`（一句自然语言主诉）、`expected_terms`（若干中文词/短语）。  
- **性质**：**人工构造的合成主诉 + 为自动评价服务的期望词表**，用于子串召回类指标；**非**多中心临床诊断金标准。  
- **规模**：当前 **N = 12** 条（小规模、便于答辩复现；可按课程要求扩充至 20～30 条并重新跑脚本）。

---

## 四、评价指标定义

对单条病例，设期望词集合为 \(T\)，词数为 \(|T|\)。

1. **检索片段—期望词召回率（单条）**  
   将本次返回的所有参考片段的标题、出处、摘要拼接为文本 \(S\)。  
   \(\text{Recall}_{\text{ret}} = \frac{1}{|T|}\sum_{t \in T} \mathbf{1}[t \text{ 为 } S \text{ 的子串}]\)。

2. **回答—期望词召回率（单条）**  
   对模型输出全文 \(A\)（取 `diagnosis` 与 `analysis` 中有内容者），  
   \(\text{Recall}_{\text{ans}} = \frac{1}{|T|}\sum_{t \in T} \mathbf{1}[t \text{ 为 } A \text{ 的子串}]\)。  
   分别对有 RAG、无 RAG 各算一次。

3. **数据集层面汇总**（脚本已算算术平均）：  
   - `mean_retrieval_term_recall`  
   - `mean_answer_term_recall_with_rag` / `mean_answer_term_recall_no_rag`  
   - `mean_delta_answer_recall` = 前者减后者（有 RAG 相对基线的平均提升）  
   - `rag_nonempty_rate`：有 RAG 时检索返回条数大于 0 的病例占比  

**局限**：子串匹配无法区分「正确医学推理」与「表面用词重合」；期望词由作者设定，存在主观性。论文中应单列「局限与展望」。

---

## 五、一次实测结果（供论文直接制表）

以下数值来自 **`eval/results/latest.json`**（生成时间 UTC：`2026-05-12T15:34:37`，本地复跑会因模型随机性略有波动）。

### 5.1 总体指标汇总

| 指标 | 符号/字段 | 数值 | 百分比（便于正文叙述） |
|------|-----------|------|------------------------|
| 病例条数 | `n_cases` | 12 | — |
| 检索片段期望词平均召回 | `mean_retrieval_term_recall` | 0.6944 | 69.44% |
| 有 RAG 时回答期望词平均召回 | `mean_answer_term_recall_with_rag` | 1.0 | 100% |
| 无 RAG 时回答期望词平均召回 | `mean_answer_term_recall_no_rag` | 0.9445 | 94.45% |
| 回答召回平均提升（有−无） | `mean_delta_answer_recall` | 0.0555 | +5.55 个百分点 |
| 检索非空比例 | `rag_nonempty_rate` | 1.0 | 100% |

**简要解读（可写入结果小节）**：在本金标准与期望词设定下，**有 RAG 时回答侧平均召回不低于无 RAG，且平均高出约 5.55 个百分点**；**12 条均成功返回非空检索片段**。检索侧平均召回约 **69.44%**，表明部分期望词未出现在检索摘要拼接文本中（可能与摘要截断、用词与语料不一致有关），可在讨论中分析。

### 5.2 分病例要点（附录或缩小字号表）

| 病例 ID | 检索召回 | 有RAG回答召回 | 无RAG回答召回 | 回答侧 Δ |
|---------|----------|---------------|---------------|----------|
| cv_chf | 1.000 | 1.000 | 1.000 | 0.000 |
| cv_chest | 1.000 | 1.000 | 0.667 | **+0.333** |
| resp_cough | 0.333 | 1.000 | 1.000 | 0.000 |
| resp_asthma | 1.000 | 1.000 | 1.000 | 0.000 |
| gi_ulcer | 0.667 | 1.000 | 1.000 | 0.000 |
| gi_liver | 0.667 | 1.000 | 1.000 | 0.000 |
| cv_htn | 0.333 | 1.000 | 1.000 | 0.000 |
| resp_infection | 1.000 | 1.000 | 1.000 | 0.000 |
| cv_palp | 0.667 | 1.000 | 1.000 | 0.000 |
| gi_reflux | 1.000 | 1.000 | 1.000 | 0.000 |
| resp_pe | 0.333 | 1.000 | 1.000 | 0.000 |
| cv_syncope | 0.333 | 1.000 | 0.667 | **+0.333** |

其中 **cv_chest**、**cv_syncope** 两条在无 RAG 基线下回答召回较低，有 RAG 后提升明显，适合作为**个案插图**（主诉 + 两条回答节选对比，注意脱敏与篇幅）。

---

## 六、论文与答辩中「如何展示」

1. **主文一张总表**：仅用 **第五节表 5.1**（五行指标），最清晰。  
2. **一张流程图**：用户输入 → 分诊/关键词 →（可选）FAISS 检索 → Prompt 拼接 → 大模型；无 RAG 基线用虚线表示「不注入摘录」即可。  
3. **附录**：完整 `per_case` 可来自 `latest.json`；或只附表 5.2 + 1～2 个典型案例的对话节选。  
4. **口头表述**：强调 **「在本评估协议与小规模金标准上，相对无 RAG 基线，回答期望词召回平均提升 …；不推广为临床最优」**，避免「全局最好」类表述。  
5. **图表文件**：可将表 5.1 复制到 Word/LaTeX。  
6. **自动生成图（推荐）**：项目内已提供脚本，从 `eval/results/latest.json` 生成 **柱状图 + 折线图**（数据与论文表一致）：
   ```bash
   .venv/bin/python eval/plot_results.py
   ```
   默认使用 **英文轴标题**（任意环境不易乱码）。若在 **macOS / Windows 且已安装中文字体** 的机器上需要中文图题，可加：
   ```bash
   .venv/bin/python eval/plot_results.py --zh
   ```
   输出路径：**`eval/results/figures/ablation_bar.png`**（三根柱：检索召回、有 RAG 回答召回、无 RAG 回答召回）、**`eval/results/figures/per_case_line.png`**（12 个病例 ID 横轴，两条折线：有/无 RAG）。可直接插入 Word。重新跑 `eval/run_ablation.py` 后请再执行本脚本以更新图片。

7. **若用 ChatGPT / 其他工具画图**：把下面整段作为提示词，并附上 `latest.json` 里 `mean_*` 与 `per_case` 片段（或表 5.1、表 5.2）即可。

---

### 附：给 AI 绘图工具的提示词（复制即用）

请根据以下数据绘制两张 **学术论文风格** 图（白底、高对比、无装饰性 emoji），用于本科毕设。

**图 A — 柱状图（总体）**  
- 三个柱子，数值分别为：检索片段期望词平均召回 **0.694**；有 RAG 时回答期望词平均召回 **1.000**；无 RAG 时回答期望词平均召回 **0.945**。  
- Y 轴范围 0～1.05，柱顶标注三位小数。  
- 建议标题：「RAG 消融实验：总体指标对比」或英文 *RAG ablation (aggregate)*。

**图 B — 折线图（逐病例）**  
- 横轴为 12 个病例 ID（顺序与 `gold_set.json` / 表 5.2 一致）：cv_chf, cv_chest, resp_cough, resp_asthma, gi_ulcer, gi_liver, cv_htn, resp_infection, cv_palp, gi_reflux, resp_pe, cv_syncope。  
- 两条折线：绿色实线圆点「有 RAG」的 `answer_term_recall_with_rag`；橙色虚线方点「无 RAG」的 `answer_term_recall_no_rag`。  
- 纵轴 0～1；浅灰网格；图例不遮挡曲线。  
- **数值必须逐条与 `eval/results/latest.json` 中 `per_case` 数组一致**（或与本文件表 5.2 一致）；勿凭记忆手写，以免与毕设正文表格不一致。

输出 PNG，分辨率不低于 300 dpi 或宽 1200 px 以上。

---

## 七、答辩可用的一句话结论

在本课题自建语料与 12 条合成主诉、期望词子串召回这一**代理指标**下，引入本地 RAG 后，**平均回答召回相对无检索基线提升 0.0555（约 5.55 个百分点）**，且检索非空率为 1；该结论**受期望词设定与样本规模约束**，不替代临床验证。

---

## 八、维护说明

- 修改 `eval/gold_set.json` 或语料后，重新运行 `eval/run_ablation.py`，将新的 `eval/results/*.json` 中的汇总行更新进 **第五节**；或在论文中改为引用固定文件名与运行日期。  
- 勿将含真实密钥的 `.env` 提交至版本库。
